{
  "name": "@sprout/cli",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "bin": {
    "sprout": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "compile": "mkdir -p ../../dist && bun build --compile --target=bun-linux-x64-musl --define SPROUT_CLI_VERSION=\\\"${SPROUT_CLI_VERSION:-dev}\\\" --outfile ../../dist/sprout-linux-x64 ./src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@sprout/api-client": "workspace:*",
    "@sprout/preview-db": "workspace:*",
    "@sprout/preview-env": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "^5.9.3"
  }
}
