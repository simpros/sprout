slug: demoapp
preview:
  hostname: "pr-{pr_id}.demoapp.preview.example.com"
health:
  path: /health
  interval: 2s
  timeout: 120s
  expect: 200
seed:
  dockerfile: Dockerfile.seed
  inputs:
    - Dockerfile.seed
    - package.json
    - scripts/seed.ts
    - docker-seed-entrypoint.sh
